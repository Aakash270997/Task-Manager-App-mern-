import React from 'react'

const StickTitle = ({title}) => {
  return (
    <div className="border-b pb-2">
      <div className="font-semibold text-zinc-800 text-center">{title}</div>
    </div>
  )
}

export default StickTitle